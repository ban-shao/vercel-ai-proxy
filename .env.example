# 服务配置
PORT=3001
AUTH_KEY=your-secret-key

# 上游 Vercel AI Gateway Host（建议仅写 host，不带 /v1 等路径）
UPSTREAM_URL=https://ai-gateway.vercel.sh

# （可选）覆盖默认上游 base
# OpenAI-compatible base (默认: ${UPSTREAM_URL}/v1)
# UPSTREAM_OPENAI_BASE_URL=https://ai-gateway.vercel.sh/v1
# AI SDK base (默认: ${UPSTREAM_URL}/v3/ai)
# UPSTREAM_AI_SDK_BASE_URL=https://ai-gateway.vercel.sh/v3/ai

# 密钥管理
KEYS_FILE=./data/keys/keys.txt
KEY_COOLDOWN_HOURS=24

# 兼容旧变量（可不配）
# COOLDOWN_HOURS=24

# 日志
LOG_LEVEL=info
LOG_DIR=./logs
